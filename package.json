{
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bundle exec jekyll serve",
    "fmt": "prettier --write \"**/*\""
  },
  "devDependencies": {
    "prettier": "2.0.5"
  }
}
